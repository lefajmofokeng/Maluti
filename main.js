// main.js

// Prevents the right-click context menu from appearing anywhere on the page
document.addEventListener('contextmenu', event => event.preventDefault());

// You can add other global, shared scripts here as well